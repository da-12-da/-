import test from 'node:test';import assert from 'node:assert/strict';
import {validateIdeas,uniform,targetRotation,pointerIndex,navigationURL} from '../public/core.js';
test('2–8 options, trimmed/nonempty/normalized duplicates and length',()=>{assert.equal(validateIdeas(['火锅','烧烤']),'');for(const list of [['火锅'],Array(9).fill('A'),['火锅','  '],['火锅',' 火锅 '],['A','ａ'],['a'.repeat(13),'b']])assert.ok(validateIdeas(list));});
test('every sector aligns with pointer after repeated spins, for 2–8 options',()=>{for(let n=2;n<=8;n++){let angle=0;for(let repeat=0;repeat<100;repeat++)for(let i=0;i<n;i++){const next=targetRotation(angle,i,n);assert.ok(next-angle>=1440);assert.equal(pointerIndex(next,n),i);angle=next;}}});
test('uniform rejection removes modulo bias',()=>{let calls=0;assert.equal(uniform(3,()=>++calls===1?4294967295:5),2);assert.equal(calls,2);for(let n=2;n<=8;n++){const counts=Array(n).fill(0);for(let i=0;i<n*100;i++)counts[uniform(n,()=>i)]++;assert.deepEqual(counts,Array(n).fill(100));}});
test('map destinations preserve correct coordinate order and name; demos blocked',()=>{const s={name:'火锅 & 小炒',location:{lng:104.08,lat:30.66,coordType:'gcj02'}};const a=new URL(navigationURL(s,'amap')),b=new URL(navigationURL(s,'baidu'));assert.equal(a.searchParams.get('to'),'104.08,30.66,火锅 & 小炒');assert.equal(b.searchParams.get('destination'),'latlng:30.66,104.08|name:火锅 & 小炒');assert.equal(b.searchParams.get('coord_type'),'gcj02');assert.throws(()=>navigationURL({...s,demo:true},'amap'));});
