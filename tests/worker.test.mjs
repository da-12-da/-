import test from 'node:test';import assert from 'node:assert/strict';
import {handle,normalizeShops,parseSearch} from '../worker/jzmd-worker.js';
import {navigationURL} from '../public/core.js';
const origin='https://test.pages.dev',env={AMAP_KEY:'test-only-not-a-real-key',ALLOWED_ORIGINS:origin};
const params=new URLSearchParams({category:'火锅',radius:'3',location:JSON.stringify({kind:'coordinates',lat:30.66,lng:104.08,coordType:'wgs84'})});
const req=(path='/api/shops?'+params,options={})=>new Request('https://jzmd.test'+path,{headers:{Origin:origin},...options});
const poi=(id,extra={})=>({id:id,name:'测试餐厅'+id,address:'测试地址',location:'104.081,30.661',biz_ext:{rating:'4.5',comment_num:'10'},...extra});
test('worker converts WGS84 and returns 3 real-shaped results, no AK',async()=>{const calls=[];const response=await handle(req(),env,async address=>{const u=new URL(address);calls.push(u);assert.equal(u.searchParams.get('key'),env.AMAP_KEY);if(u.pathname==='/v3/assistant/coordinate/convert')return Response.json({status:'1',locations:'104.08,30.66'});return Response.json({status:'1',pois:[poi('a'),poi('b'),poi('c'),poi('d')]});});const text=await response.text(),data=JSON.parse(text);assert.equal(response.status,200);assert.equal(data.shops.length,3);assert.equal(data.shops[0].location.coordType,'gcj02');assert.equal(data.shops[0].rating,4.5);assert.ok(!text.includes(env.AMAP_KEY));assert.equal(calls[0].searchParams.get('coordsys'),'gps');assert.equal(calls[1].searchParams.get('types'),'050000');assert.equal(calls[1].searchParams.get('radius'),'3000');assert.equal(calls[1].searchParams.get('keywords'),'火锅');assert.equal(response.headers.get('Access-Control-Allow-Origin'),origin);});
test('manual location uses geocoding and handles empty results',async()=>{const q=new URLSearchParams(params);q.set('location',JSON.stringify({kind:'region',label:'成都 春熙路'}));let count=0;const r=await handle(req('/api/shops?'+q),env,async address=>{if(++count===1){assert.equal(new URL(address).pathname,'/v3/geocode/geo');return Response.json({status:'1',geocodes:[{location:'104.08,30.66'}]});}return Response.json({status:'1',pois:[]});});assert.deepEqual((await r.json()).shops,[]);});
test('distance filtering, rating ordering and missing fields',()=>{const items=normalizeShops([poi('a',{biz_ext:{}}),poi('far',{location:'105,31'}),poi('b',{biz_ext:{rating:5}}),poi('b')],{lat:30.66,lng:104.08},3000);assert.deepEqual(items.map(x=>x.id),['b','a']);assert.ok(!('rating' in items[1]));assert.ok(!('positiveRate' in items[0]));});
test('CORS rejects unknown origins, permits preflight and never exposes secrets',async()=>{assert.equal((await handle(req('/api/config',{headers:{Origin:'https://evil.test'}}),env)).status,403);assert.equal((await handle(req('/api/shops',{method:'OPTIONS'}),env)).status,204);assert.deepEqual(await(await handle(req('/api/config'),env)).json(),{configured:true});const r=await handle(req(),{ALLOWED_ORIGINS:origin});assert.equal(r.status,503);});
test('invalid input, quota errors, malformed JSON and rate limiting',async()=>{assert.throws(()=>parseSearch(new URL('https://jzmd.test/api/shops?radius=999')));const q=await handle(req(),env,async()=>Response.json({status:'0',infocode:'10003',message:env.AMAP_KEY}));assert.equal(q.status,429);assert.ok(!(await q.text()).includes(env.AMAP_KEY));assert.equal((await handle(req(),env,async()=>new Response('bad'))).status,502);assert.equal((await handle(req(),{...env,RATE_LIMITER:{limit:async()=>({success:false})}})).status,429);});
test('Baidu navigation uses exact BD09 coordinate and address; Amap converts',()=>{const s={name:'店',address:'地址',location:{lat:30.66,lng:104.08,coordType:'bd09ll'}};const b=new URL(navigationURL(s,'baidu'));assert.equal(b.searchParams.get('coord_type'),'bd09ll');assert.equal(b.searchParams.get('destination'),'latlng:30.66,104.08|name:店 · 地址');assert.ok(!new URL(navigationURL(s,'amap')).searchParams.get('to').startsWith('104.08,30.66,'));});
