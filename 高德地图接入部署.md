# 高德地图接入

当前 JZMD Worker 使用高德 Web 服务，已替换百度商家检索。前端界面及两个地图入口保持原样。旧《百度地图接入部署.md》仅为历史记录，不适用于当前 Worker。

在 Cloudflare → jzmd → Settings → Variables and Secrets 中添加 **Secret `AMAP_KEY`**，填写高德 **Web 服务**类型的 Key，保存并部署。不要使用 JavaScript API Key，不要填写到 BAIDU_AK。Key 仅由服务端请求使用，不进入前端或仓库。

后端通过 `/v3/assistant/coordinate/convert` 将浏览器 GPS 坐标转换为 GCJ-02；手动区域通过 `/v3/geocode/geo` 解析；周边商家使用 `/v3/place/around`，餐饮类型 `050000`、距离排序、最多获取 25 个候选。再次过滤半径，按评分优先、距离次之返回最多 3 家。只显示接口实际返回的评分，不推算评价数量或好评率。

官方文档：https://lbs.amap.com/api/webservice/guide/api-advanced/search 。账号需具备周边搜索、地理编码与坐标转换权限及可用额度。配置 Secret 不等于权限验证成功，需通过实际查询确认；服务拒绝和额度耗尽返回明确错误，不回退到假数据。

发布与后续更新见 [GitHub-Pages部署.md](GitHub-Pages部署.md)。运行 `node --test tests/*.test.mjs` 和 `node tests/worker-browser.mjs` 验证适配层；测试使用虚构响应，与线上真实检索分开。
