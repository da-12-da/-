# 百度地图真实商家：Pages 前端 + JZMD Worker

## 交付和当前状态

完整可粘贴的 Worker 源码：`worker/jzmd-worker.js`，单文件模块，没有外部依赖。前端已接入公开 Worker 地址配置、自动定位、真实字段展示、BD-09 百度导航及高德坐标转换；页面布局、转盘和五页流程保留。

**当前尚未提供现有 JZMD 的 URL/源码，也未连接 Cloudflare 账户。** 因此没有修改线上 Worker、没有取得或使用真实 AK，不能声称已验证真实百度商家或真机导航。`deployment.json` 中地址暂为空，当前 dist 明确显示未接入。补齐公开 URL、部署 Worker 代码并配置 Secret 后，按下面步骤联调即可。测试数据仅存在于 tests，不作为正式推荐。

## 一、在已有 JZMD Worker 设置 AK

1. 登录 Cloudflare 控制台，选正确账户，打开 **Workers & Pages → JZMD**。
2. 打开 **Settings → Variables and Secrets → Add**，选择 **Secret** 类型。
3. 名称准确填写 `BAIDU_AK`，值粘贴你已取得的百度 Web 服务端 AK，然后保存并按界面提示部署变更。
4. AK 不写到 Worker 源码、Pages 环境变量、deployment.json、URL 参数或 public/dist，也不要发到聊天里。代码只从 `env.BAIDU_AK` 取值。
5. 再添加一个 **Text** 变量 `ALLOWED_ORIGINS`。本地先填写 `http://127.0.0.1:5173,http://localhost:5173`；Pages 创建后追加实际生产来源，例如 `https://你的项目.pages.dev`。多个来源用英文逗号分隔，不带末尾斜杠或路径。绑定自有域名后再追加它的准确来源。

这是运行时变量配置，不是 Workers Builds 的构建变量。Secret 保存后通常不再显示原值。已有 AK 的百度账户还须确认地点检索 2.0、地理编码、坐标转换 v2 的权限、配额和访问限制；本项目不能代替账户实测。

官方：[Worker Secrets](https://developers.cloudflare.com/workers/configuration/secrets/)。

## 二、部署完整 Worker 代码

1. 打开本项目 `worker/jzmd-worker.js`，复制完整内容。
2. 在 **JZMD → Edit code** 中使用模块格式粘贴，点击 **Deploy**。入口是文件底部的 `export default`，无需安装包。
3. 若现有 JZMD 还有其他业务路由，先保留其代码，将本文件的 `/api/config` 与 `/api/shops` 路由合并进去，不直接覆盖其他功能。由于尚未取得旧源码，本次未完成这一步合并。
4. 从 Worker 的概览或 Domains & Routes 复制实际 HTTPS 地址。
5. 将公开地址填写到项目根目录 `deployment.json`：

```json
{"workerBaseUrl":"https://这里替换为实际JZMD域名"}
```

只填来源：`https://域名`，不能包含 `/api/shops`、AK、查询参数或登录信息。此地址是公开 API 地址，不是秘密。构建会自动生成 `dist/api/config` 并把准确的 Worker 来源加入 `dist/_headers` 的 CSP。

### Worker 协议及数据行为

- `GET /api/config`：只返回 `{configured: true/false}`，不返回 AK。CORS 来源必须获准。
- `GET /api/shops`：参数为 `category`、`radius`（3/5/10 公里）、`location`（JSON 字符串）。
- 定位输入：`{"kind":"coordinates","lat":30.66,"lng":104.08,"coordType":"wgs84"}`。Worker 调用 `/geoconv/v2/` 的 `model=2`，将 GPS 转为百度 BD-09。
- 手动输入：`{"kind":"region","label":"成都 春熙路"}`。Worker 调用 `/geocoding/v3/` 得到 BD-09 查找中心。
- 地点检索调用 `/place/v2/search`，关键词为抽中的品类，`tag=美食`、`scope=2`、`coord_type=3`、`radius_limit=true`，请求最多 20 个距离优先候选。
- Worker 再按查找中心计算直线距离，剔除范围外结果和重复 uid；候选按评分优先、距离次之排序，缺失评分排后，最多返回 3 家。并非覆盖附近全部商家。
- 返回名称、地址（若提供）、BD-09 坐标、计算距离、评分及评价数量（若提供）。不补造缺失字段，不推算好评率。评分来自 `detail_info.overall_rating`。
- 返回中的 `distanceMeters` 是以查找中心计算的直线距离，不是驾车路程。
- 非零百度状态、配额错误、超时、无效 JSON 和位置解析失败，均返回简短的错误码/提示，不回传百度原始错误或含 AK 的上游 URL。

### 密钥保护与额度保护

Worker 只访问固定的百度地址，不接受任意代理目标；前端请求无需 AK 或 Secret。限制请求长度、品类长度、半径和坐标值，拒绝未授权来源，支持 OPTIONS，并对错误响应返回 CORS 头。

内置每实例每 IP 30 次/分钟的基础限流，同时支持名为 `RATE_LIMITER` 的 Cloudflare Rate Limiting 绑定（使用 `env.RATE_LIMITER.limit`）。生产建议配置该绑定/平台限流并监测百度账户配额。CORS 不是身份认证，非浏览器客户端可以伪造 Origin；不能把隐藏 AK 等同于代理绝不会被盗刷。内存限流不是跨实例的全局配额。[平台限流文档](https://developers.cloudflare.com/workers/runtime-apis/bindings/rate-limit/)。

## 三、本地真实联调

先完成线上 JZMD 代码部署、Secret、ALLOWED_ORIGINS 和 deployment.json，之后在本项目目录执行：

```powershell
node server.mjs
```

打开 `http://127.0.0.1:5173`。若服务已占用该端口，先停止旧进程再启动；本地配置不放 AK，查询仍由线上 JZMD 代理。

1. 添加至少两个餐饮选项 → GO → 自动进入结果页。
2. 点击“看看附近的…店”，首次查询会请求浏览器定位。授权后浏览器把 WGS-84 位置发到 JZMD。
3. 在开发者工具 Network 中检查：`/api/config` 返回公开 Worker 地址；商家请求为 `https://实际JZMD域名/api/shops?...`，应返回真实百度数据。
4. 确认请求不包含 AK，响应不含 Secret；浏览器不直接请求百度 Place API。导航按钮跳到百度网站属于导航，不是浏览器携 AK 调用 POI。
5. 选择店铺 → “就选这家” → 查看名称地址距离 → 默认百度地图 → 点击导航。手机尝试 `baidumap://`，未成功切入 App 且页面仍可见时回退至百度 HTTPS 路线页。需在实体手机上验证安装/未安装客户端两种情况；内置浏览器可能限制唤起。
6. 百度使用原始 BD-09 坐标；高德入口保留，转换至 GCJ-02，不能把 BD-09 当 GCJ-02 原样传入。

### 可复制的接口排查

直接在浏览器地址栏打开 Worker 通常没有 Origin，本实现将返回 403，这是预期的来源限制。应从获准前端查询，或用以下不含 AK 的命令（替换真实 Worker 地址）：

```powershell
$workerBase = 'https://实际JZMD域名'
Invoke-RestMethod -Uri "$workerBase/api/config" -Headers @{ Origin='http://127.0.0.1:5173' }
$locationValue = [uri]::EscapeDataString('{"kind":"region","label":"成都 春熙路"}')
$categoryValue = [uri]::EscapeDataString('火锅')
Invoke-RestMethod -Uri "$workerBase/api/shops?category=$categoryValue&radius=3&location=$locationValue" -Headers @{ Origin='http://127.0.0.1:5173' }
```

403 检查 ALLOWED_ORIGINS；503 检查 BAIDU_AK Secret；429 检查限流/百度额度；502 检查百度接口权限和可用性。`configured:true` 只表示 Secret 存在，不保证权限/额度正确，必须实际检索一次。

## 四、打包并直接上传 Pages

填写真实 Worker 地址后，在「就这么定」目录运行：

```powershell
node build.mjs
```

有 npm 时等价于 `npm run build`。无需安装依赖。产物仍为以下 8 个文件：

```text
dist/index.html
dist/app.js
dist/core.js
dist/style.css
dist/favicon.svg
dist/success.wav
dist/_headers
dist/api/config
```

1. Cloudflare **Workers & Pages → Create application → Pages / Get started → Direct Upload / Drag and drop**。
2. 输入项目名称，不绑定 GitHub。
3. 上传整个 dist，根层级直接包含 index.html，保留 `_headers` 和 api 子目录。不要上传 worker、server.mjs、测试或 .env。
4. 点击 **Deploy site / Save and Deploy**，等待成功，复制实际生产 `https://项目名.pages.dev` 链接。
5. 把此准确来源追加进 JZMD 的 `ALLOWED_ORIGINS`，保存部署；不使用 `*.pages.dev` 或任意来源。
6. 页面、音效由 Pages 提供，POI 查询由 Worker 提供，百度 AK 只在 Worker Secret 中。Pages 不需要添加 BAIDU_AK，也不部署 Functions。

[Cloudflare Direct Upload 官方步骤](https://developers.cloudflare.com/pages/get-started/direct-upload/)。

## 五、部署验收

- 电脑无痕窗口打开 pages.dev，完成录入、GO、结果、定位、真实商家与确认流程；刷新仍能打开。结果和指针一致，GO 不重复触发。
- iPhone Safari、Android Chrome 通过 Wi-Fi 与移动网络各测试一次；竖屏、横屏均无横向滚动和转盘裁切。
- 拒绝定位后更改区域继续查询；测试 3→5→10 公里，旧选店清空；零结果正常提示，不足 3 家显示实际数量。
- 模拟断网或测试 Worker 错误，确认有错误提示、可重试，不伪造推荐。
- Network 中核对商家请求走 JZMD；页面源码、JS、dist、浏览器请求/响应中无 AK；没有 CSP/CORS 报错。
- 商家评分缺失时不展示评分，地址缺失时不补造；百度地图终点坐标与返回商家坐标一致。移动端真实唤起须人工检查。

本次自动测试包括：16 项算法/适配/Worker 测试、原流程浏览器回归，以及“前端→自动定位→跨域 Worker→百度适配→商家确认→BD09 导航”的本地集成测试。百度上游在测试中被明确替身替换，没有把测试响应当真实商家。尚未完成真实 AK/远端 Worker/pages.dev 的验收。

## 六、后续更新

修改 public 源码 → `node build.mjs` → 原 Pages 项目 **Create a new deployment → Production** → 上传完整新 dist → Deploy。生产链接不变，Worker Secret 不受前端更新影响。

修改 Worker 时单独更新 JZMD 代码并部署，保留 Secret 和来源白名单；只有地址或前后端契约变化时才需同步修改前端配置并重新打包。

测试命令：

```powershell
node --test tests/core.test.mjs tests/provider.test.mjs tests/worker.test.mjs
node tests/browser.mjs
node tests/worker-browser.mjs
```

浏览器测试需要 Playwright 和 Chromium/Edge。`tests/dist.mjs` 目前验证未配置地址时静态版的降级路径；真实地址配置后请使用上述人工真实联调步骤，不把模拟测试当成真实接口验收。

## 百度官方接口依据

- [圆形地点检索](https://lbs.baidu.com/docs/webapi?title=placev2%2Fguide%2Fwebservice-placeapi%2Fcircle)
- [地理编码](https://lbs.baidu.com/docs/webapi?title=geocoding%2Fguide%2Fwebservice-geocoding-base)
- [坐标转换 v2](https://lbsyun.baidu.com/docs/webapi?title=geoconv%2Fguide%2Fchangeposition-base)
- [百度 iOS 调起](https://lbsyun.baidu.com/docs/webapi?title=mapadjustment%2Furi%2Fios)
- [百度 Android 调起](https://lbsyun.baidu.com/docs/webapi?title=mapadjustment%2Furi%2Fandriod)
