# 商家接口配置

## 接入前先确认

当前没有供应商凭证，以下项目尚未实测。上线前由接口账户负责人确认并记录：

1. 餐饮关键词/周边检索、城市区域解析、坐标转换的可用权限。
2. 可返回的店名、详细地址、坐标、评分及其量表、评价数量字段；好评率是否由来源独立提供。
3. 展示、排序、缓存、来源标注等使用权限。
4. 每日额度、QPS、费用、限流错误码；在实际账户完成请求、零结果与超额测试。
5. 输出位置坐标系和区域中心的准确性，在真实地点核对地图目标。

评分只接受 0–5 分量表；不得把评分换算成好评率。其他量表须明确规范化为 5 分并得到数据使用许可。无字段用 null，不能补造评价数。

## 配置

复制 `.env.example` 为 `.env`。仅当以上条件已确认并完成网关联调后设置：

```dotenv
PROVIDER_VERIFIED=true
MERCHANT_API_URL=https://your-controlled-gateway.example/nearby
MERCHANT_API_TOKEN=your-server-side-token
```

网关实现及部署由数据供应商接入方提供。密钥仅用于服务端 Authorization 头，不进入浏览器。不要提交 `.env`。`/api/config` 只返回是否配置，不返回 URL 或密钥。变更配置后重启 Node 服务。

## 网关请求契约

POST 到配置的固定 HTTPS URL，10 秒超时、不跟随重定向，头包含 `Authorization: Bearer <token>` 和 `Content-Type: application/json`。

```json
{
  "category": "火锅",
  "radiusMeters": 3000,
  "location": {"kind": "region", "label": "成都 · 春熙路"},
  "limit": 50
}
```

定位成功时 location 为：

```json
{"kind":"coordinates","lng":104.08,"lat":30.66,"coordType":"wgs84"}
```

以上坐标仅用于解释协议。浏览器定位使用 WGS-84，网关必须正确转换，不能只修改 coordType 标签。手动位置须解析为真实区域中心，不可直接把城市文字当坐标。区域不明确或不可解析时返回错误，让用户输入更具体的区域。

## 网关返回契约

```json
{
  "source": "实际数据来源名称",
  "category": "火锅",
  "center": {"lng":104.08,"lat":30.66,"coordType":"gcj02"},
  "shops": []
}
```

shops 中每项必须包含：

- `id`：唯一字符串。
- `name`、`address`：非空字符串；详细地址用于用户确认。
- `location`：`{lng, lat, coordType: "gcj02"}`，真实商家坐标。
- `rating`：0–5 的数值或 null。
- `reviewCount`：非负整数或 null。
- `positiveRate`：来源明确提供的 0–100 数值或 null。

网关需确保返回商家匹配请求品类、来自同一可比较的评分体系。返回 category 必须与请求一致。网关可以返回最多 200 个候选，常规建议最多 50 个；应用按中心与商家的 GCJ-02 坐标计算直线距离、剔除半径外数据、按 id 去重，再按评分降序、评价数量降序、距离升序返回最多 3 家，缺失评分排后。距离标注为「距查找位置」，不是导航道路距离。

不能保证该排序覆盖供应商未返回的商家，因此界面明确标注只是来源返回候选中的最多三家。来源未提供好评率时整行隐藏，不从 rating 推算。

网关非 2xx、网络超时、无效 JSON、坐标系不符或字段非法均成为明确错误；不会以空数组掩盖接口失败，不会把原始错误正文或密钥传给前端。空数组只表示成功查询且没有结果。

## 导航依据

高德使用其 [URI 路径规划入口](https://developer.amap.com/api/uri-api/guide/travel/route)：终点顺序为经度、纬度、名称，`mode=car`，`callnative=1`。移动端可由地图获取起点，第三方内置浏览器可能限制唤起。

百度使用其 [Web URI 路线规划入口](https://lbsyun.baidu.com/index.php?title=%E6%A8%A1%E6%9D%BF%3Auriapi%E7%9A%84web%E7%AB%AF)：终点 `latlng:纬度,经度|name:名称`，明确 `coord_type=gcj02`，`mode=driving`、`output=html`。原型路线不参与导航。

链接参数已通过自动测试；真实账户数据、具体手机浏览器、已安装地图客户端和实际行程仍需联调验收。
